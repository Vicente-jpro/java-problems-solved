spring:
  application:
    name: msclientes
  
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      platform: postgres
      url: jdbc:postgresql://localhost:5432/mydb
      username: foo
      password: bar
      driverClassName: org.postgresql.Driver
